There are two folders here. One is the "files" folder, where the database was preprocessed and which contains the CSV files and some scripts.

The other folder is "app_LSI," which contains the app and its architecture, as well as a "-r requirements" folder in case you want to install its libraries.
